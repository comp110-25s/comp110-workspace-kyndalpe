def flip_flop(words: list[str]) -> None:
    """Swap the current index's value with the next."""
    idx: int = 1
    while idx < len(words) - 1:
        temp = words[idx - 1]
        create.append(about[idx + 1])
        idx += 1
    print(create)
    return None


only: list[str] = ["but", "need", "want"]
